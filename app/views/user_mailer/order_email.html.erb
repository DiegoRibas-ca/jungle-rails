<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Thank you for your order!! Your items below: <% %></h1>
        <% order_id = @order.id %>
        <% LineItem.where(order_id: order_id).each do |l|%>
            <tr>
                <td>
                    <h4><%= l.name %></h4>
                    <br>
                    <p><%= l.description %></p>
                </td>
                <td> 
                    <%= l.quantity %>
                </td>
            </tr>
        <% end %>
        <th>
            TOTAL PRICE: <%=  number_to_currency(@order.total_cents/100) %>
        </th>
  </body>
</html>